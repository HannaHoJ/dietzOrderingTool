import { Meteor } from 'meteor/meteor';
import '/imports/server/methods/methods.js';
import '/imports/server/publish/publish.js';
import '/imports/server/data.js';

Meteor.startup(() => {
  // code to run on server at startup
});
